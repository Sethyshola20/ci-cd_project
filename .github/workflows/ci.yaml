name: CI

on:
  push:
    branches:
      - main  # Déclencher la CI sur les pushs sur la branche main
  pull_request:
    branches:
      - main  # Déclencher la CI sur les pull requests vers la branche main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '22.3.0'

      - name: Install dependencies
        run: npm install

      - name: Run ESLint
        run: npm run lint

      - name: Build application
        run: npm run build

       - name: Run Cypress tests
        run: |
          npm install cypress
          npx cypress run

      - name: Upload coverage report to Codecov
        uses: codecov/codecov-action@v2
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          file: ./coverage/lcov.info
          flags: unittests

      - name: Run SonarQube analysis
        uses: sonarsource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: Deploy to staging
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          echo "Deploying to staging environment..."
          # Ajouter ici les commandes pour déployer vers votre environnement d'intégration (par exemple Kubernetes)
